<?xml version="1.0"?>
<!--
  Mission: DROP → HOME
  start_id = 2 (DROP)
  goal_id  = 0 (HOME)
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <Sequence name="drop_to_home">

      <!-- Set fixed route IDs -->
      <SetBlackboard value="2" output_key="my_start_id"/>
      <SetBlackboard value="0" output_key="my_goal_id"/>

      <!-- Phase 1: Compute route on route graph -->
      <ComputeRoute
        start_id="{my_start_id}"
        goal_id="{my_goal_id}"
        use_start="false"
        use_poses="false"
        path="{path}"
        route="{route}"/>

      <!-- Phase 2: Follow route to endpoint -->
      <FollowPath
        path="{path}"/>

      <!-- Phase 3: Compute path to exact PoseStamped goal -->
      <ComputePathToPose
        goal="{goal}"
        path="{path}"/>

      <!-- Phase 4: Follow last-mile path -->
      <FollowPath
        path="{path}"/>

    </Sequence>
  </BehaviorTree>
</root>
